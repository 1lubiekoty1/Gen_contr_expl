make some actual way of generating contrfacts
my idea:
 - make a convolutional neural network that takes an image and a class and a model and then
   tries make a new image that gives a different class
 - it will have loss function as a BCELoss (for making a different class) and
   ImageDifferenceLoss ( for how much image has been altered ), but the first one must be more important